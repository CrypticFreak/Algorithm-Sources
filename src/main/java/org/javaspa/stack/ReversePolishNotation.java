package org.javaspa.stack;

import java.util.Enumeration;

/**
 * Created by Ankur K on 5/9/14.
 * Question : Evaluate the value of an arithmetic expression in Reverse Polish Notation.
 * Valid operators are +, -, *, /. Each operand may be an integer or another expression.
 * Some examples:
 * ["2", "1", "+", "3", "*"] -> ((2 + 1) * 3) -> 9
 * ["4", "13", "5", "/", "+"] -> (4 + (13 / 5)) -> 6
 * Online Judge : http://oj.leetcode.com/problems/evaluate-reverse-polish-notation/
 * Accepted : No
 *
 * Recursive solution
 */

public class ReversePolishNotation {
/*
  private class RPNToken{

    final boolean isOperator;
    RPNToken(boolean isOperator){
      this.isOperator = isOperator;
    }

    private enum Operator {
      add, subtract, divide, multiply
    }

  }
  public int evalRPN(String[] tokens) {
    return 0;
  }*/
}
