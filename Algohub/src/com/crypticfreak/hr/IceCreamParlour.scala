package com.hr

/**
 * Created by Ankur K on 1/16/15.
 */
object IceCreamParlour {
  def main(args: Array[String]) {/*
    val numTestCases = readInt()
    (1 to numTestCases).foreach(p => {
      val money = readInt()
      val icecreams = readInt()
      val flavours = readLine().split(" ").map(_.toInt).sortBy(_)
      var left = 0
      var right = icecreams - 1
      while(left <= right)
    })*/
  }
}
